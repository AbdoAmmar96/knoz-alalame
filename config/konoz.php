<?php

return [

    /*
    |--------------------------------------------------------------------------
    | جذر الويب الفعلي
    |--------------------------------------------------------------------------
    | يُضبط فقط في النشر المقسوم: كود المشروع خارج جذر الويب ومحتوى public
    | منسوخ إلى مجلد الدومين. يستخدمه konoz:deploy --check ليعرف أين يبحث
    | عن رابط storage.
    |
    | لا تقرأ env() مباشرة خارج ملفات الإعدادات — تُرجع null بعد config:cache.
    */

    'public_docroot' => env('PUBLIC_DOCROOT'),

];
